To do

- verify:
  - dictionary is valid
  - inputs/outputs match
- schema
  - core
    - if/else
    - tuple accessor
    - struct accessor
    - math (add, subtract)
- build core integration
  - if/else
  -
- use graph lib to represent program in memory
